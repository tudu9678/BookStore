spring.thymeleaf.cache=false

# Set config database connection

spring.datasource.url=jdbc:mysql://localhost:3306/bookstoredatabase
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# User name and pwd
spring.datasource.username=root
spring.datasource.password=tienhung1996
# Keep the connection if idle for a long time (NEEDED in product)
# spring.datasource.tomcat.test-while-idle=true
# spring.datasource.tomcat.validation-query=SELECT 1

# Log sql statements and their parameters
spring.jpa.show-sql=true
# Hibernate ddl auto ( update ,create-drop,create)
spring.jpa.hibernate.ddl-auto = update

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
# for reload server
spring.devtools.restart.poll-interval=2s
spring.devtools.restart.quiet-period=1s
# debug=true
spring.datasource.dbcp2.test-while-idle=true
spring.datasource.dbcp2.validation-query=select 1


spring.mail.host=smtp.gmail.com
spring.mail.username=xalmon1405@gmail.com
spring.mail.password=Chiyeuminhemthoi1.
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.failback=false
spring.mail.properties.mail.smtp.starttls.enable=true